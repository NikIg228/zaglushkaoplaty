services:
  - type: web
    name: test-payment-backend
    env: python
    buildCommand: ""
    startCommand: "uvicorn full_mock_payment_server:app --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true